# 檢查、加入並提交所有變更
git status
git add -A
git commit -m "你的描述性 commit 訊息"

# 先同步遠端最新（建議使用 rebase，避免多餘 merge）
git fetch origin
git pull --rebase origin dev

# 若有衝突，解決後再 continue；衝突解完後：
# git add <files>
# git rebase --continue

# 最後 push
git push origin dev
# 若第一次設定上游
# git push -u origin dev